[pytest]
# One command for the Python half: `cd pipeline && python3 -m pytest`.
# rootdir is pipeline/, which is also the documented cwd for every CLI here, so
# `tam.*` imports and the relative default paths behave the same as in a real run.
testpaths = tests
addopts = -q
filterwarnings =
    error::DeprecationWarning:tam.*
