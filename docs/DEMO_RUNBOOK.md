# คู่มือเดโม — ทำอะไรบ้าง ตามลำดับ

เอกสารนี้คือสิ่งที่ต้องทำจริงในวันเดโม เรียงตามเวลาที่ทำ ไม่ใช่ตามความสำคัญของฟีเจอร์
ตัวเลข beat ตรงกับ `/meowtam demo` ในโค้ด ([slack-bot/src/app.ts](../slack-bot/src/app.ts))
ถ้าเอกสารนี้กับโค้ดไม่ตรงกัน **โค้ดถูก** — พิมพ์ `/mt demo` แล้วอ่านจากหน้าจอ

ค่าที่ตั้งไว้อยู่ตอนนี้ (จาก `.env` ทั้งสองฝั่ง):

| ของ | ค่า |
| --- | --- |
| ช่องที่บอทโพสต์ | `#test-meow` (`DIGEST_CHANNEL`) — `DAILY_CHANNEL` ว่าง จึงใช้ช่องเดียวกัน |
| คนที่ได้ standup DM | `STANDUP_USERS` 2 คน |
| งานตามเวลา | **ปิดอยู่** (`ENABLE_SCHEDULE` ว่าง) — ทุกอย่างยิงด้วยมือผ่าน `/mt demo` |
| ข้อมูลงาน | จาก pipeline `http://127.0.0.1:8899` (`TAM_API_URL`) ไม่ใช่ fixture ของบอท |
| เขียนลง YouTrack | **เปิด** ฝั่ง pipeline (`YOUTRACK_WRITE=1`) → บอทเขียนคอมเมนต์จริงผ่าน pipeline |
| เกณฑ์งานเงียบ | 5 วันทำการ (`TAM_STALE_WORKDAYS`) |
| เกณฑ์เรื่องค้างซ้ำ | 3 เช้า (`TAM_DAILY_PENDING_DAYS` default) |

---

## 1. ก่อนขึ้นเวที 30 นาที

### สามคำสั่ง จบ

```bash
cd /Users/sirawith/Desktop/AIM/scraping-slack-data
./demo.sh reset      # ล้างของรอบซ้อม (สำรองให้ก่อนอัตโนมัติ)
./demo.sh up         # ให้แน่ใจว่า dashboard + bot รันอยู่ แล้วสรุปสถานะให้ดู
./demo.sh share      # เปิด URL สาธารณะให้คนอื่นเปิดดู dashboard ได้ (ข้ามได้ถ้าไม่ต้องการ)
```

แล้วใน Slack พิมพ์ `/mt demo clear` กับ `/mt demo reset` — สองอันนี้อยู่ในหน่วยความจำของบอทและในเธรด สั่งจากเชลล์ไม่ได้

`./demo.sh` เฉย ๆ = ดูสถานะทุกอย่างในหน้าจอเดียว (dashboard ตอบไหม, บอทรันอยู่ไหม, อ่านจาก pipeline หรือ fixture,
งานตามเวลาปิดอยู่ไหม, มีข้อมูลรอบซ้อมค้างกี่รายการ, URL สาธารณะเปิดอยู่หรือเปล่า) — คำสั่งเดียวที่ควรพิมพ์ก่อนขึ้นเวที
คำสั่งอื่นที่มี: `restart` (หลังแก้ `.env` หรือแก้โค้ด) · `restore` (เอาข้อมูลก่อน reset กลับ) · `unshare` · `logs [dashboard|bot|tunnel]`

ที่เหลือในหัวข้อนี้คือ *สคริปต์นั้นทำอะไรให้บ้าง* และวิธีทำมือเมื่อไม่อยากใช้สคริปต์

### ล้างของเก่าทิ้งก่อน — ทำเป็นอย่างแรก

รอบซ้อมทิ้งของไว้สี่ที่: ประวัติ daily, บันทึกว่า "เคยประกาศไปแล้ว", decision ที่กดเก็บไว้ และ corpus
ที่โดน `paste` เขียนเพิ่ม ถ้าไม่ล้าง beat 3 จะบอกว่าวันนี้โพสต์ไปแล้ว และ beat 1 จะไปเขียนทับเช้าของเมื่อวาน

**สำรองก่อนเสมอ** — ไฟล์พวกนี้เป็นของจริงของทีม (คำตอบ daily ที่คนพิมพ์เอง) ลบแล้วไม่มีใครสร้างคืนให้

```bash
cd /Users/sirawith/Desktop/AIM/scraping-slack-data

mkdir -p .demo-snapshot
cp slack-bot/data/dailies.json slack-bot/data/announcements.json \
   slack-bot/data/decisions.json .demo-snapshot/ 2>/dev/null
cp pipeline/data/link_overrides.json pipeline/data/processed/real_all.json .demo-snapshot/
```

แล้วล้าง — บอทอ่านไฟล์พวกนี้ใหม่ทุกครั้งที่ใช้ จึงแก้ระหว่างบอทรันอยู่ได้ **ไม่ต้อง restart**:

```bash
printf '[]\n' > slack-bot/data/dailies.json        # ประวัติ daily ทุกวัน → beat 1 เริ่มจากศูนย์ beat 3 โพสต์ใหม่ได้
printf '[]\n' > slack-bot/data/announcements.json  # "เคยประกาศแล้ว" → /mt stale พูดเรื่องเดิมได้อีก
printf '[]\n' > slack-bot/data/decisions.json      # decision ที่กด 📌 ไว้ตอนซ้อม
```

แล้วใน Slack:

```
/mt demo clear      ลบเช้าจำลอง + คำตอบจำลองที่ค้างอยู่ในเธรดของจริง
/mt demo reset      ตัวนับ beat กลับไป 1 (ตัวนับอยู่ในหน่วยความจำ restart บอทก็รีเซ็ตเหมือนกัน)
```

**ถ้าอยากเก็บ daily ของจริงไว้** ลบเฉพาะของวันนี้ก็พอ แค่นี้ beat 1 กับ beat 3 ก็ทำงานได้แล้ว:

```bash
python3 - <<'PY'
import json, datetime, pathlib
p = pathlib.Path('slack-bot/data/dailies.json')
today = datetime.date.today().isoformat()
rows = json.loads(p.read_text(encoding='utf-8'))
kept = [r for r in rows if r.get('date') != today]
p.write_text(json.dumps(kept, ensure_ascii=False, indent=2), encoding='utf-8')
print(f'{len(rows)} วัน → เหลือ {len(kept)}')
PY
```

**ถ้าซ้อม beat 8 (paste) ไปแล้ว** corpus โดนเขียนจริง — ย้อนจาก snapshot แล้วสั่ง build ใหม่:

```bash
cp .demo-snapshot/real_all.json pipeline/data/processed/real_all.json
cp .demo-snapshot/link_overrides.json pipeline/data/link_overrides.json
curl -s -X POST -H "X-TAM-Token: $(grep '^TAM_ADMIN_TOKEN=' pipeline/.env | cut -d= -f2-)" \
     127.0.0.1:8899/api/reindex
```

ล้างด้วยคำสั่งไม่ได้ มีสามอย่าง:

| ของ | ต้องทำมือ |
| --- | --- |
| ข้อความที่บอทโพสต์ไปแล้วใน `#test-meow` | ลบใน Slack เอง — บอทไม่มีคำสั่งลบข้อความตัวเอง |
| คอมเมนต์ที่เขียนลง YouTrack ตอน beat 8/9 | ลบใน YouTrack |
| DM ที่ส่งไปแล้วตอน beat 2 | ลบในห้อง DM นั้น |

เช็กว่าสะอาดจริงก่อนขึ้นเวที: `cat slack-bot/data/dailies.json` ต้องได้ `[]`
และใน Slack พิมพ์ `/mt demo` ต้องขึ้น *beat ถัดไป: 1.*

### เซิร์ฟเวอร์สองตัว — ปกติรันอยู่แล้ว

เครื่องนี้ลง `deploy/install.sh` ไว้ **dashboard กับบอทจึงขึ้นเองตั้งแต่ login และ launchd เปิดใหม่ให้ถ้าตาย**
ไม่ต้องเปิดเทอร์มินัลค้างไว้เลย เช็กด้วย `./demo.sh` หรือ `launchctl list | grep com.tam`

ถ้าจะรันมือ (เครื่องอื่น หรือปิด launchd ไว้):

```bash
# เทอร์มินัล 1 — pipeline (ต้องขึ้นก่อน บอทอ่านจากตัวนี้)
cd pipeline
python3 -m tam.web.server --records data/processed/real_all.json --days 7 --port 8899
```

รอจนขึ้น `INFO Ready: … record(s), … topic(s), … blocked` แล้วค่อยเปิดบอท
ถ้าเปลี่ยน corpus ให้ชี้ `--records` ไปไฟล์ที่เพิ่ง build (ตัวที่มี embedding cache คู่กันจะขึ้นเร็ว ไม่ต้อง embed ใหม่)

`--days` คือหน้าต่างของ digest ไม่ใช่ขนาด corpus — **7 คือค่าที่ควรใช้กับข้อมูลจริง** เพราะ digest แปลว่า
"อะไรขยับบ้างสัปดาห์นี้" ค้นหายังเจอทั้ง corpus อยู่ดี (`real_all.json` มีข้อความตั้งแต่ 8 มิ.ย. ถึงเมื่อวาน
อยู่ในเจ็ดวันหลังสุด 43 ข้อความ) เลข `--days 3650` ที่เห็นใน `pipeline/README.md` มีไว้สำหรับ
`sample_combined.json` ซึ่งวันที่เก่ามาก ถ้าไม่เปิดหน้าต่างให้กว้างจะไม่เหลืออะไรใน digest เลย

**เช็กก่อนว่ามีตัวที่รันอยู่แล้วหรือยัง** — เปิดซ้ำจะได้ `Address already in use` เฉย ๆ ไม่ได้พัง:

```bash
curl -s 127.0.0.1:8899/api/health     # ตอบมา = มีตัวรันอยู่แล้ว ใช้ตัวนั้นได้เลย
```

ดูสามค่าในคำตอบ: `records_path` ต้องเป็น corpus ที่ตั้งใจ, `window_days` คือ `--days` ที่ตัวนั้นเปิดไว้,
`built_at` คือเวลาที่ index ถูกสร้าง ถ้าสามอันนี้ถูกก็ไม่ต้องรีสตาร์ท

```bash
# เทอร์มินัล 2 — บอท
cd slack-bot
npm start
```

ต้องเห็นบรรทัดสรุปตอนบูต ที่บอกจำนวน work item + จำนวนข้อความ + ว่าอ่านจาก **pipeline** ไม่ใช่ fixture
ถ้าขึ้นว่า fixture แปลว่า `TAM_API_URL` ไม่ติด — หยุดแล้วแก้ก่อน อย่าเดโมทับ

### เช็ก 4 อย่าง

```bash
./demo.sh                              # 1. สถานะทุกอย่างในหน้าจอเดียว — ต้องเขียวหมด
cd slack-bot && npm run check-api      # 2. บอทคุยกับ pipeline ได้จริง + gate ของ recall ตั้งถูกกับ corpus นี้
```

3. ใน Slack พิมพ์ `/mt` → ต้องได้บอร์ด (บอทมีชีวิต + คำสั่งลงทะเบียนแล้ว)
4. `/mt demo clear` แล้ว `/mt demo reset` → ให้ beat 1 เริ่มจากศูนย์

### จัดหน้าจอ

| หน้าต่าง | ใช้ตอน |
| --- | --- |
| Slack — ช่อง `#test-meow` เลื่อนลงล่างสุด | beat 3–7, 9, 10 |
| Slack — DM ของบอท (หน้าต่างที่สอง) | beat 2 |
| เบราว์เซอร์ — dashboard `127.0.0.1:8899` | ตอนตอบคำถามว่าเบื้องหลังคิดยังไง |
| เบราว์เซอร์ — YouTrack ticket ที่จะโดนเขียน | beat 8, 9 (ไปยืนยันคอมเมนต์ที่บอทเขียน) |
| เบราว์เซอร์ — `docs/deck.html` | ตอนเปิดและตอนปิด |
| URL สาธารณะจาก `./demo.sh share` | ส่งให้คนดูเปิดเองระหว่างที่เราพูด |

> ถ้าซ้อมวันเดียวกันไปแล้ว: `/mt demo clear` ลบเช้าจำลองและคำตอบจำลองออกหมด **แต่โพสต์ daily ของวันนี้เป็นของจริง จึงยังอยู่**
> beat 3 จะตอบว่า "วันนี้โพสต์ไปแล้ว" พร้อมบอกวิธี — ถ้าจะเริ่มเธรดใหม่ให้พิมพ์ `/mt daily post again` ก่อนขึ้นเวที

---

## 2. ลำดับเดโม 11 beat

พิมพ์ `/mt demo next` ไปเรื่อย ๆ หรือระบุเลข `/mt demo 5` ถ้าจะข้าม
`/mt demo` เฉย ๆ = ดูว่าอยู่ beat ไหน และแต่ละ beat ตรงกับคำสั่งจริงตัวไหน

**beat 1–7 คือเช้าเดียวเรียงตามนาฬิกา** 08:45 → 09:00 → คนตอบ → 09:25 → 09:30 → 10:45
เป็นลำดับเดียวกับที่ `scheduleDaily` ยิงจริงทุกวัน จึงไม่มีจังหวะไหนย้อนเวลา
**beat 8–11 คือของที่ใช้ตอนไหนก็ได้** ไม่มีนาฬิกามาบังคับ

| # | พิมพ์ | จะเกิดอะไร | ประโยคที่พูด |
| --- | --- | --- | --- |
| 1 | `/mt demo 1` | เขียนประวัติ daily ย้อนหลัง 2 เช้าทำการ (= `PENDING_DAYS - 1`) คนแรกใน `STANDUP_USERS` ติดเรื่องเดิมทุกเช้า **เขียนคนละสำนวน** | "สองเช้าที่ผ่านมาเกิดขึ้นแล้ว คนเดิม เรื่องเดิม แต่พิมพ์ไม่เหมือนกันสักวัน" |
| 2 | `/mt demo 2` | **08:45** DM ไปหาทุกคนใน `STANDUP_USERS` พร้อมร่างของเมื่อวาน + ปุ่ม *ส่ง* / *ข้ามวันนี้* | "มันไม่ถามว่าเมื่อวานทำอะไร มันบอกแล้วให้ผมแก้" |
| 3 | `/mt demo 3` | **09:00** โพสต์ daily ลงห้อง ยกเรื่องที่ค้างจากเมื่อวานขึ้นหัวโพสต์ | "ไม่ต้องถามว่าอะไรค้าง มันรู้อยู่แล้วและเอาขึ้นบนสุด" |
| 4 | `/mt demo 4` | ใส่คำตอบจำลอง 3 คนเข้าเธรดวันนี้ + โพสต์บอกในเธรดว่าเป็นของจำลอง | "ใครพิมพ์ของจริงในเธรดนี้ ตอนสรุปของจริงชนะของจำลองเสมอ" |
| 5 | `/mt demo 5` | **09:25** digest ลงห้อง ที่ติดขึ้นก่อน ทุกข้ออ้างกดดูข้อความต้นทางได้ | "ที่ติดขึ้นก่อน ทุกบรรทัดห่างจากข้อความที่พิสูจน์มันหนึ่งคลิก" |
| 6 | `/mt demo 6` | **09:30** งานที่ไม่มีใครแตะเกิน 5 วันทำการ → ประกาศในห้อง | "ไม่มีใครยกมือ ไม่มีใครพูดถึงเลยด้วยซ้ำ — คนละโรคกับงานที่ติด และมีแต่การนับเท่านั้นที่เจอ" |
| 7 | `/mt demo 7` | **10:45** อ่านเธรดทั้งหมด สรุปตอบในเธรด + ถ้าบรรทัดไหนค้างครบ 3 เช้า ประกาศในห้องหนึ่งครั้ง | "เช้าที่สามแล้ว มันพูดครั้งเดียว แล้วไม่พูดเรื่องเดิมอีก" |
| 8 | `/mt demo 8` | เปิดฟอร์มแนบแชท พร้อมตัวอย่างแชทจาก DM ใส่มาให้ → เลือก ticket → *อ่านให้ดูก่อน* → *เก็บเข้า corpus* | "การตัดสินใจเกิดใน DM ที่ไม่มี token ไหนเข้าถึง วางมันลงไป เลือก ticket แล้วมันไปอยู่ใต้ ticket นั้นจริง" |
| 9 | `/mt demo 9` | การ์ด "Slack กับ ticket ไม่ตรงกัน" จากการเทียบของจริง → กด *ดูร่างที่เสนอ* → *บันทึกลง YouTrack* | "requirement เปลี่ยนตรงนี้ ticket ไม่เปลี่ยน มันเห็น แล้วมันเขียนคอมเมนต์ลง ticket จริง" |
| 10 | `/mt demo 10` | recall คำถามภาษาไทย พร้อมสายการตัดสินใจ (อันเก่าถูกอันใหม่ทับ) | "พฤษภาเราบอกไม่เอา BOM สิงหาเราเปลี่ยน ticket ยังเป็นของพฤษภา" |
| 11 | `/mt demo 11` | บอร์ดรวม (เห็นคนเดียว) | ปิดท้าย |

### รายละเอียดที่ต้องรู้ก่อนกดแต่ละ beat

**beat 1** — ถ้าไม่ได้ตั้ง `STANDUP_USERS` จะไม่ทำอะไรเลยแล้วบอกว่าให้ตั้งก่อน
ข้อมูลทุกชิ้นติดป้าย `(จำลอง)` ทุกที่ที่แสดง และลบออกได้ด้วย `/mt demo clear` — **พูดออกไปตรง ๆ ว่าอันนี้จำลอง** ดีกว่าให้เขาถาม

**beat 2** — ต้องมี DM ของบอทเปิดค้างไว้ในหน้าต่างที่สอง ไม่งั้นจะเสียเวลาหาบนเวที
ใครไม่มี draft (ไม่ได้เป็น participant ของ work item ไหนเลย) บอทจะบอกชื่อคนนั้นตรง ๆ ว่าไม่มี ไม่ได้เงียบหาย
กดปุ่ม *ส่ง* ให้ดูหนึ่งครั้ง — ถ้ากรอกช่อง blocker ไว้ มันจะโพสต์ blocker ใหม่เข้าห้องทันที

**beat 3** — ถ้าวันนี้โพสต์ไปแล้ว มันจะไม่โพสต์ซ้ำ (เธรดจะแตกสองเธรด) แต่จะตอบว่าโพสต์ไปแล้วพร้อมลิงก์
จะเริ่มใหม่จริง ๆ ต้อง `/mt daily post again`

**beat 4** — บอท **ไม่โพสต์แทนคนอื่น** คำตอบจำลองถูกเก็บลง record แล้วโพสต์แจ้งในเธรดว่าเป็นของจำลอง
ถ้าคนในห้องอยากพิมพ์จริงในเธรดนั้น ทำได้เลย ตอน beat 7 ของจริงจะทับของจำลองของคนคนนั้น (คนอื่นยังใช้ของจำลองอยู่)

**beat 6** — **ไม่ปั้นข้อมูล** ถ้าไม่มีงานไหนเงียบครบ 5 วันทำการจริง มันจะบอกว่าไม่มี แล้วบอกว่างานที่เงียบที่สุดตอนนี้คืออะไร เงียบมาถึงเมื่อไร
อันนี้เป็นจุดขาย ไม่ใช่จุดพลาด — พูดออกไปว่า "มันไม่ยอมปั้นตัวเลขให้ผมโชว์"

**beat 7** — สรุปจะบอกด้วยว่าในนั้นมีคำตอบจำลองกี่อัน การประกาศเรื่องค้างจะยิงก็ต่อเมื่อบรรทัดเดิมโผล่ครบ 3 เช้าจริง ๆ (beat 1 เขียนไว้ 2 เช้า + วันนี้ = 3)

**beat 8** — ต้องพิมพ์จาก slash command เท่านั้น (ต้องมี trigger_id ถึงจะเปิด modal ได้)
ตอนกด *เก็บเข้า corpus* คำตอบที่ได้เป็นผลลัพธ์ ไม่ใช่คำอวยพร: เขียนลงไฟล์ไหน, comment id ใน YouTrack เท่าไร, และ **ledger ที่ build ใหม่แล้วมีข้อความพวกนั้นอยู่ใต้ ticket นั้นกี่ข้อความ** — ถ้าเป็นศูนย์มันบอกศูนย์ ตรงนี้ช้าหลายสิบวินาที (embed + cluster ใหม่) เตรียมพูดคั่นไว้

**beat 9** — ตอนนี้ `TAM_API_URL` ตั้งอยู่ ดังนั้นจะได้ **การ์ด drift จากการเทียบ Slack กับ YouTrack จริง** ไม่ใช่ nudge ในเธรดแบบ fixture
(`DEMO_FIXTURES=1` ที่ตั้งไว้ไม่มีผลกับ beat นี้ เพราะ drift ของ fixture อ้างข้อความของ fixture ซึ่งกดตามไม่ได้ จึงถูกตัดทิ้ง — นี่คือพฤติกรรมที่ถูก)
ถ้าไม่มี drift เลย มันจะบอกว่าไม่มีทั้งสองแหล่งและบอกว่าแต่ละแหล่งว่างเพราะอะไร

**beat 10** — คำถามที่ใช้ถูก hardcode ไว้ (เรื่อง export encoding) โพสต์ลงห้อง ถ้าอยากถามสด ใช้ `/mt recall <คำถาม>` แทน

---

## 3. เดโมแต่ละอันเท่ากับ action ไหนของจริง

ตารางนี้ครบทุก action ที่มีใน Slack — อันที่เขียน `—` คือของจริงที่ใช้ได้ แต่ไม่ต้องเสียเวลาบนเวที

| Action จริง | beat |
| --- | --- |
| `/mt` · `/mt @ชื่อ` · `/mt REVERAPP-140` | 11 |
| `/mt blocked` | 11 *(digest ที่ beat 5 ก็เรียงที่ติดขึ้นก่อนอยู่แล้ว)* |
| `/mt daily` (ฟอร์ม เห็นคนเดียว) | 4 |
| `/mt daily post` / `daily post again` | 3 |
| `/mt daily summary` | 7 |
| `/mt digest` | 5 |
| `/mt stale` / `stale post` | 6 |
| `/mt paste` | 8 |
| `/mt drift` | 9 |
| `/mt silent` (`quiet`) | — *ticket ที่เปิดค้างและไม่มีใครแตะ อ่านจาก `/api/tracker` เดียวกับ beat 9 คนละครึ่งของรายงาน* |
| `/mt recall <คำถาม>` · พิมพ์อะไรที่ไม่ใช่คำสั่ง | 10 |
| `/mt projects` | — *ห้องไหนคือโปรเจกต์อะไร ถ้าเมนูเลือก ticket ดูแปลก ให้พิมพ์อันนี้ก่อน beat 8* |
| `/mt format` (`help`) | — *สอนรูปแบบที่ระบบอ่านได้ ส่งให้ทีมอ่านเอง ไม่ต้องเดโม* |
| `/mt reload` | — *อ่านข้อมูลใหม่โดยไม่ restart ใช้ตอนกลางเดโมได้* |
| เมนู ⋯ ที่ข้อความ → **ผูกกับ ticket** | 8 *(ทางเขียนเดียวกัน: link override → คอมเมนต์ YouTrack → build ใหม่)* |
| เมนู ⋯ → **บันทึกเป็นการตัดสินใจ** · 📌 · 🧠 | — *อันนี้แหละที่เติมสายการตัดสินใจให้ beat 10 อ่าน* |
| 🎫 · 🚧 · ✅ | — *เป็นคำใบ้เฉย ๆ ไม่เขียนอะไร และมันบอกเองว่าไม่ได้เขียน* |
| ปุ่ม **ส่ง** / **ข้ามวันนี้** ใน standup DM | 2 |
| ปุ่ม **ดูข้อความ** / **ที่มา** ทุกข้ออ้าง · **เปิด** / **ดูงานนี้ในบอร์ด** ทุกการ์ด | 5, 11 |
| ปุ่ม **ดูร่างที่เสนอ** → modal *อัปเดต ticket* → **บันทึกลง YouTrack** | 9 |
| ปุ่ม **ไม่ใช่การเปลี่ยนสโคป** | 9 *(พูดด้วยว่ามันแค่ log ไว้ ยังไม่ได้เก็บถาวร — ตัวบอทก็ตอบแบบนั้น)* |
| **อ่านให้ดูก่อน** → **เก็บเข้า corpus แล้วผูกกับ …** / **ยกเลิก** | 8 |
| งานตามเวลา 08:45 · 09:00 · 09:25 · 09:30 · 10:45 (`ENABLE_SCHEDULE=1`) | 2 · 3 · 5 · 6 · 7 — beat พวกนี้**คือ**งานตัวเดียวกัน แค่ยิงด้วยมือ |

---

## 4. คำสั่งเดโม

```
/mt demo          ดูว่า beat ถัดไปคืออะไร + ทั้ง 11 beat ตรงกับคำสั่งจริงตัวไหน
/mt demo next     ยิง beat ถัดไป
/mt demo 6        ยิง beat ที่ระบุ (ข้ามได้)
/mt demo reset    กลับไป beat 1 (ไม่แตะข้อมูล)
/mt demo clear    ลบเช้าจำลองและคำตอบจำลองทั้งหมด
```

`/mt` คือชื่อย่อของ `/meowtam` ใช้บนเวทีเถอะ สั้นกว่า และ autocomplete ของ Slack ที่ `/me` บางทีอืด

---

## 5. ลิงก์ที่ใช้ตอนนำเสนอ

**URL สาธารณะ** — `./demo.sh share` คืนลิงก์ `https://<สุ่ม>.trycloudflare.com` ที่ชี้มาที่ dashboard ของเครื่องนี้
ส่งให้คนในห้องเปิดเองได้ระหว่างที่เราพูด ไม่ต้องต่อจอ ลิงก์เปลี่ยนทุกครั้งที่เปิดใหม่ และ **ไม่มีรหัสผ่านกั้นฝั่งอ่าน**
(route ที่เขียนข้อมูลยังต้องใช้ `X-TAM-Token` เหมือนเดิม) ในนั้นมีข้อความ Slack จริง ถ้าไม่อยากให้ชื่อจริงขึ้นจอ
ตั้ง `TAM_NAMES=pseudonym` ทั้ง `pipeline/.env` และ `slack-bot/.env` แล้ว `./demo.sh restart` · จบแล้วปิดด้วย `./demo.sh unshare`

**Dashboard** (ต้องเปิด `tam.web.server` ไว้)

| ลิงก์ | ใช้ตอบคำถามว่า |
| --- | --- |
| <http://127.0.0.1:8899/> | สรุปงาน — เห็นทุก work item ที่ขยับ |
| <http://127.0.0.1:8899/blockers> | เฉพาะที่ติด พร้อมข้อความที่พิสูจน์ |
| <http://127.0.0.1:8899/people> | รายคน — ใครอยู่กับงานไหน |
| <http://127.0.0.1:8899/tracker> | เทียบกับ ticket — อันเดียวกับที่ beat 9 อ่าน |
| <http://127.0.0.1:8899/search> | วางประโยค แล้วดูว่าเบื้องหลังดึงอะไรมา |
| <http://127.0.0.1:8899/upload> | เพิ่มโน้ต/transcript เข้า corpus |
| <http://127.0.0.1:8899/api/health> | เอาไว้พิสูจน์ว่าต่อจริง ตอนใครสงสัยว่า mock |

**API ที่หยิบมาโชว์ได้เร็ว** — `curl 127.0.0.1:8899/api/digest` · `/api/tracker` · `"/api/search?q=…&k=5"` (ตัวหลังคืน `relevance.lexical` กับ `relevance.dense` ซึ่งคือ gate ของ recall จริง ๆ)

**เอกสารสำหรับฉาย** (เปิดด้วย `open docs/<ไฟล์>`)

| ไฟล์ | คือ |
| --- | --- |
| [docs/deck.html](deck.html) | เด็คหลัก เปิดกับปิดด้วยอันนี้ |
| [docs/OVERVIEW.html](OVERVIEW.html) | ภาพรวมหน้าเดียว ส่งให้คนที่ไม่ได้ดูเดโม |
| [docs/architecture.html](architecture.html) | เครื่องในของระบบ ไว้ตอบคำถามสถาปัตยกรรม |

**เอกสารไว้ตอบคำถามลึก**

| ไฟล์ | ใช้ตอบ |
| --- | --- |
| [docs/USER_MANUAL.md](USER_MANUAL.md) | ติดตั้งใช้งานจริงยังไงทั้งระบบ |
| [docs/DAILY_USE.md](DAILY_USE.md) | ทีมใช้ทุกวันยังไง คำสั่งไหนใช้ตอนไหน |
| [docs/CALCULATION.md](CALCULATION.md) · [docs/GLOSSARY.md](GLOSSARY.md) | ตัวเลขแต่ละตัวคำนวณจากอะไร ศัพท์แต่ละคำแปลว่าอะไร |
| [docs/EXPERIMENTS.md](EXPERIMENTS.md) · [pipeline/README.md](../pipeline/README.md) | ทำไมเลือก `BAAI/bge-m3`, fine-tune แล้วแพ้ base ยังไง, gate ของ recall ทำงานยังไง |
| [slack-bot/README.md](../slack-bot/README.md) | ฝั่งบอท: อะไรจริงอะไรจำลอง ตารางเดโม |

**ของนอกเครื่อง**

- YouTrack: <https://blntech.youtrack.cloud> โปรเจกต์ `REVERAPP` — เปิด ticket ที่จะโดนเขียนค้างไว้ พอ beat 9 เขียนเสร็จให้ refresh ให้ดูสด ๆ
- Slack app config: <https://api.slack.com/apps> — ใช้ตอนโดนถามเรื่อง scope/permission

---

## 6. จบเดโมแล้ว

```bash
./demo.sh unshare    # ปิด URL สาธารณะ — ทำก่อนอย่างอื่น ลิงก์ยังเปิดอยู่คือคนยังเข้าได้
./demo.sh restore    # เอาข้อมูลก่อน reset กลับมา (daily ของจริง, decision, corpus) แล้ว build index ใหม่ให้
```
```
/mt demo clear     ลบข้อมูลจำลองทั้งหมด บอกจำนวนเช้าและจำนวนคำตอบที่ลบ
```

ทำทันทีหลังจบ ไม่ต้องรอ — ข้อมูลจำลองที่ค้างไว้ข้ามวันคือข้อมูลจำลองที่จะกลายเป็นข้อมูลจริงโดยไม่มีใครตั้งใจ
ส่วนคอมเมนต์ที่เขียนลง YouTrack ตอน beat 8/9 **เป็นของจริงและลบไม่ได้จากตรงนี้** ถ้าไม่อยากให้ค้าง ไปลบใน YouTrack เอง

---

## 7. พังตรงไหน ทำยังไง

| อาการ | เพราะ | ทำ |
| --- | --- | --- |
| บอทบูตแล้วบอกว่าอ่านจาก fixture | `TAM_API_URL` ไม่ติด หรือ pipeline ยังไม่ขึ้น | เปิด pipeline ก่อน แล้ว restart บอท |
| `Address already in use` ตอนเปิด pipeline | มี `tam.web.server` รันอยู่แล้วที่ port นั้น | `curl -s 127.0.0.1:8899/api/health` ดูว่าโหลด corpus ถูกไหม ถูกก็ใช้ตัวเดิม · จะเปลี่ยนค่าค่อย `lsof -nP -iTCP:8899 -sTCP:LISTEN` เอา PID ไป `kill` แล้วเปิดใหม่ |
| บอทไม่ยอมบูต บอกว่าอ่าน pipeline ไม่ได้ | ตั้งใจให้เป็นแบบนั้น — ถ้าบูตต่อจะเสิร์ฟข้อมูลเก่าที่หน้าตาเหมือนของสด | เช็กว่า `python3 -m tam.web.server` ยังรันอยู่ที่ port 8899 |
| beat 3 บอกว่าโพสต์ไปแล้ว | ซ้อมไปแล้ววันนี้ | `/mt daily post again` |
| beat 6 บอกว่าไม่มีงานเงียบพอ | ไม่มีจริง ๆ และมันไม่ปั้น | พูดออกไปว่านี่คือพฤติกรรมที่ถูก แล้วอ่านตัวที่เงียบที่สุดที่มันบอกมา |
| beat 8 ค้างนาน | กำลัง embed + cluster ใหม่ทั้ง corpus | รอ มันจะตอบเป็นผลลัพธ์ตอนเสร็จ อย่ากดซ้ำ |
| beat 9 บอกว่าไม่มี drift | ไม่มีเรื่องที่ Slack กับ ticket ไม่ตรงกันจริง ๆ | ข้ามไป beat 10 หรือใช้ `/mt silent` โชว์ ticket ที่เงียบแทน |
| ข้อมูลใน Slack ไม่ตรงกับ dashboard | บอท cache ไว้ตั้งแต่บูต | `/mt reload` (ไม่ต้อง restart) |
| ชื่อคนขึ้นเป็น `U0…` | ยังไม่ได้ดึงชื่อ | `python3 -m tam.ingest.users --fetch` แล้ว `/mt reload` |

**อัดวิดีโอสำรองไว้** เดโม Slack สดพังได้ทุกทาง — wifi งาน, rate limit, workspace เด้ง login
วิดีโอที่เล่นได้มีค่ามากกว่าเดโมสดที่ไม่ขึ้น
