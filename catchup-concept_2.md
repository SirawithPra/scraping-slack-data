# 🐱 Catchup — Web App · Slack Tools · Dashboard

**คอนเซปต์เล่ม 2 · ผิวสัมผัสทั้งสามชั้น และการต่อกับ backend ที่มีอยู่จริง**
Example Company Limited · ร่างวันที่ 17 ส.ค. 2026

> เล่ม 1 (`catchup-concept_1.md`) ตอบว่า *บอทชื่ออะไร พูดจาแบบไหน มีคำสั่งอะไร*
> เล่มนี้ตอบว่า *ของทั้งหมดวางอยู่บนผิวไหน ใครเห็นอะไร และโค้ดที่มีวันนี้ใช้ต่อได้แค่ไหน*
> ทุกตัวเลข ทุก field ในเอกสารนี้อ้างจากรีโปนี้จริง — `digest.py`, `relations.py`,
> `retrieve.py`, `meetings.py`, `server.py`, `docs/fixtures/*.json` ไม่มีอันไหนสมมติขึ้น

> **เอกสารนี้เป็นหนึ่งในสองข้อเสนอที่ใช้ backend ก้อนเดียวกัน** — Catchup พูดกับ**ทีม dev**
> ส่วน `tam-proposal.md` (*ตาม*) พูดกับ **PO** และทำสิ่งที่ Catchup ไม่ทำ เช่น เขียนกลับเข้า
> YouTrack เส้นแบ่งว่าอะไรใช้ร่วมกันและอะไรเป็นของใครอยู่ใน **§13** — อ่านข้อนั้นก่อน
> ถ้าจะเอาสองเอกสารไปเทียบกัน

---

## 1. กฎแบ่งผิว — ข้อเดียว จำง่าย

ปัญหาคลาสสิกของเครื่องมือแนวนี้คือทำ**สามอย่างซ้ำกันบนสามที่** สุดท้ายไม่มีที่ไหนสมบูรณ์
Catchup แบ่งด้วยกฎเดียว:

> **Slack คือที่ที่งาน "เกิด" · Web คือที่ที่งาน "ถูกอ่าน" · Dashboard คือที่ที่มีคน "ลงมือ"**

| ถ้าผู้ใช้กำลัง… | ไปที่ | เพราะ |
|---|---|---|
| พิมพ์ 1–2 บรรทัดว่าวันนี้ทำอะไร | Slack | ไม่มีใครเปิดเว็บเพื่อพิมพ์ประโยคเดียว |
| อยากรู้ว่าเมื่อวานทีมติดอะไร ก่อนเข้า standup 09:30 | Web (`/`) | ต้องกวาดตา 10 การ์ดใน 3 นาที Slack ทำไม่ได้ |
| ตามว่าเรื่องนี้เริ่มจากอะไร ใครตอบ ใครปิด | Web (`/item/{key}`) | เป็น timeline ที่มีทิศทาง ไม่ใช่ feed |
| วางเมาส์ไม่ได้ กำลังเดินเข้าห้องประชุม | Web บนมือถือ 390px | หน้า digest คือหน้าที่ต้อง responsive ที่สุด |
| ต้องเลือกว่าวันนี้จะเข้าไปช่วยเรื่องไหนก่อน | Dashboard (`/herding`) | เป็นคิว ไม่ใช่รายงาน |
| อยากเห็นข้อความต้นฉบับที่บอทอ้าง | Slack (deep link กลับ) | ต้นฉบับอยู่ที่เดิมเสมอ เว็บไม่ใช่ที่เก็บความจริง |

ข้อสุดท้ายสำคัญกว่าที่เห็น: **เว็บไม่เคยเป็นแหล่งความจริง** มันเป็นเลนส์
ทุกการ์ด ทุกบรรทัดหลักฐาน ต้องมีทางกลับไปหาข้อความจริงใน Slack ในหนึ่งคลิก
วันที่เว็บเริ่มเก็บข้อมูลที่ Slack ไม่มี (คอมเมนต์ในเว็บ, สถานะที่กดในเว็บ) คือวันที่
ทีมต้องเปิดสองที่ — และเครื่องมือแบบนั้นตายทุกตัว

---

## 2. หน่วยข้อมูลกลาง — work item ไม่ใช่ message

ทั้งสามผิวพูดถึงของชิ้นเดียวกัน: **work item** = คลัสเตอร์ข้อความ 5–15 ข้อความ
ที่คร่อมทั้ง Slack และ transcript ประชุม สร้างจาก `graph.py` (Louvain communities
บนกราฟที่รวมทุกสัญญาณ) แล้วให้ `digest.py` ใส่สถานะ

### กุญแจของ work item — ticket เมื่อมี, cluster เมื่อไม่มี

ตัดสิน 17 ส.ค. 2026 · รับแนวมาจากข้อเสนอ *ตาม* แล้วแก้จุดที่มันแคบเกินไป

`graph.py` จัดคลัสเตอร์แบบไม่มีครูสอน จุดแข็งคือ**เห็นงานที่ไม่มีใครเปิด ticket** —
ซึ่งเป็นครึ่งหนึ่งของงานจริงในทีม แต่พอมีคนพิมพ์ `REV-1421` ลงในข้อความ การเดาก็ไม่มีเหตุผล
ticket key คือคำตอบที่ถูกอยู่แล้ว และ `signals.py:46` จับ pattern นี้อยู่แล้วพร้อม IDF
ที่สูงจนเกือบชี้ขาด — แค่ยังไม่ได้ยกมันขึ้นเป็น *กุญแจ*

ลำดับการผูกข้อความเข้า work item เรียงจากมั่นใจที่สุด:

| ลำดับ | วิธี | ผลลัพธ์ | ของที่มีอยู่แล้ว |
|---|---|---|---|
| 1 | ข้อความมี ticket key ตรง ๆ (`REV-1421`) | ผูกแข็ง key = `ticket:REV-1421` | `signals.anchors()` |
| 2 | อยู่ใน thread เดียวกับข้อความที่ผูกแล้ว | สืบทอดจากข้อความแม่ | thread signal ใน `signals.py` |
| 3 | คลัสเตอร์เดียวกันและคะแนนเหนือเกณฑ์ | ผูกอ่อน แสดงว่าเป็นการเดา | `graph.py` + `retrieve.py` |
| 4 | คนแก้เอง | override ถาวร ใช้จูน threshold | **ยังไม่มี ต้องเพิ่ม** |

- **work item ที่ไม่มี ticket ไม่ใช่สถานะบกพร่อง** มันมี key เป็น `cluster:<id>`
  ที่ใช้งานได้เต็มที่ และมีปุ่ม "เปิด ticket จากเรื่องนี้" ที่ promote เป็น `ticket:REV-1421`
  โดยไม่เสีย timeline ที่สะสมมา
- **กองที่ผูกไม่ได้ต้องมองเห็น** — digest ต้องมีบรรทัดท้ายว่า "3 เธรดที่ยังจับคู่ไม่ได้"
  ของที่หายเงียบคือของที่ทำให้คนเลิกเชื่อทั้งหน้าจอ
- **ใครก็แก้ link ได้ ไม่ใช่แค่ lead** ถ้ามีคนเดียวที่แก้ได้ เราก็แค่ย้ายงานจัดระเบียบ
  ไปกองไว้บนคนคนนั้น ซึ่งคือปัญหาเดิมในรูปใหม่

### สถานะของ *งาน* กับสถานะของ *คน* คือของสองอย่าง

จุดที่เล่ม 1 ยังปนกันอยู่ และต้องแยกให้ชัดตั้งแต่วันนี้:

| | สถานะงาน (work item) | สถานะคน (cat state) |
|---|---|---|
| มาจาก | `relations.py` → typed relation ล่าสุด | การกระทำของคนนั้น (`/nap`, `/loaf`) |
| ค่าที่เป็นไปได้ | `blocked` · `resolved` · `active` | purring · loafing · hissing · napping · stray |
| ใครเป็นเจ้าของ | เรื่อง | คน |
| ใช้ที่ไหน | การ์ดใน digest, dashboard, ตัวเลขทุกตัว | Slack status, มุมขวาการ์ด update, App Home |
| **ห้าม** | เอาไปตัดสินคน | เอาไปคิดเป็นตัวเลขรวมของทีม |

`hissing` ของคน ≠ `blocked` ของงาน — คนคนเดียวอาจกำลัง hiss กับเรื่องที่ระบบยัง
มองว่า `active` เพราะ relation ยังไม่ถูก type และนั่นคือ *ข้อมูล* ที่มีค่า
(ระบบยังไม่เห็นสิ่งที่คนเห็นแล้ว) ไม่ใช่บั๊กที่ต้องกลบด้วยการ sync สองค่าเข้าหากัน

### สถานะงานถูก "คำนวณ" ไม่ใช่ "เดา"

จาก `digest.py:infer_state()` — relation ที่มีน้ำหนักคือ `resolves`, `blocked_by`,
`duplicates`, `follows_up`, `answers` และ **ตัวที่ชนะคือตัวที่เกิดล่าสุด**:

```
blocked_by (อังคาร)  →  ไม่มีอะไรตามมา         →  blocked   อายุ = นับจากอังคาร
blocked_by (อังคาร)  →  resolves (พฤหัส)       →  resolved
```

`age_days` ไม่ใช่ "อายุของเรื่อง" แต่คือ **"ติดมานานเท่าไร"** — นับจาก `state_since`
ไม่ใช่จากข้อความแรก ทั้ง UI ต้องใช้คำว่า "ติดมา 2 วัน" ไม่ใช่ "เก่า 2 วัน"

**ความสัมพันธ์ชนิดที่หก ที่ยังไม่มีและควรมี: `supersedes`** — วันนี้ `relations.py`
มี `duplicates` (เรื่องเดียวกัน) และ `follows_up` (ต่อจากกัน) แต่ไม่มีอะไรบอกว่า
*ข้อตกลงใหม่ยกเลิกข้อตกลงเก่า* ซึ่งเป็นความสัมพันธ์ที่ตอบคำถาม "ตกลงกันไว้ 3 เดือนก่อน
แล้วตอนนี้เปลี่ยนไปแล้วหรือยัง" ได้ตรงที่สุด · ของชิ้นนี้อยู่ใน backend ที่ใช้ร่วมกัน
และ *ตาม* ใช้มันหนักกว่า Catchup (§13) แต่ Catchup ก็ได้ประโยชน์: work item ที่ถูก
supersede แล้วไม่ควรโผล่ใน digest เป็น `active` ต่อไป

และทุกสถานะมาพร้อม `evidence` + `evidence_id`:

```json
"state": "blocked",
"evidence": "blocked since 2026-08-14 16:31 — cue “ยังรอ”",
"evidence_id": "mtg_20260814-0930-daily-standup_1786699865.000000"
```

**นี่คือเครดิตทั้งหมดของสินค้า** ประโยคเดียวนี้บอกว่าใครคำนวณ คำนวณจากอะไร และ
ข้อความไหนเป็นหลักฐาน — ต้องแตะได้ในหนึ่งคลิกจากทุกผิว รวมถึงจากการ์ดใน Slack

---

## 3. Slack tools — สามชั้น ไม่ใช่กองคำสั่ง

เล่ม 1 ลิสต์คำสั่งไว้ 15 ตัว เล่มนี้จัดมันเป็นสามชั้นตามว่า **ใครเป็นคนเริ่ม**
ชั้นที่คนต้องพิมพ์เองคือชั้นที่แพงที่สุด ควรมีน้อยที่สุด

### ชั้น A — Capture (คนเริ่ม)

| Slack surface | ทำอะไร | endpoint / โมดูล |
|---|---|---|
| `/meow <ข้อความ>` | update บรรทัดเดียว ไม่เปิด modal | `POST /slack/command` → บันทึกเป็น record ทรงเดียวกับ `messages.json` |
| `/meowmeow` | เปิด modal 4 ช่อง (เมื่อวาน / วันนี้ / blocker / mood) พร้อม pre-fill | `views.open` + `views.submit` |
| `/hiss <ปัญหา>` | แจ้ง blocker ตรง ๆ | เขียน record + tag ให้ `relations.py` type เป็น `blocked_by` ได้ง่าย |
| `/nap <วันที่>` | ลา/OOO | ไม่เข้า corpus แต่กัน stray detection |
| Message shortcut "สรุป thread นี้" | เท่ากับ `/yarn` แต่ไม่ต้องพิมพ์ | `conversations.replies` → `summarize.py` |
| ส่งไฟล์ `.vtt/.srt/.txt/.json` เข้า DM บอท | อัปโหลด transcript | `meetings.parse_transcript()` — รับสี่นามสกุลนี้อยู่แล้ว |
| **reaction** 🎫 ผูก ticket · 🚧 ติด · ✅ ปิด · 📌 เป็นการตัดสินใจ | แตะเดียว ไม่ต้องพิมพ์ | `reactions:read` → เข้า `signals.py` เป็นสัญญาณชนิดที่ไม่ใช่ข้อความ |

> **reaction คือช่องทาง capture ที่ถูกที่สุดในลิสต์นี้** เพราะทีมกด reaction อยู่แล้วทุกวัน
> และมันเป็นสัญญาณที่ *คนตั้งใจส่ง* ต่างจากการเดาจากข้อความ — 🚧 หนึ่งครั้งมีค่าเท่ากับ
> cue phrase ที่เดาถูกสิบครั้ง แต่มีเงื่อนไขว่า**ต้องประกาศชุด emoji ให้ทีมรู้ตั้งแต่วันแรก**
> ไม่ใช่ให้ค่อย ๆ เดาเอาเองว่าอะไรใช้ได้

> **modal pre-fill คือฟีเจอร์ที่คนจะพูดถึง** ไม่ใช่ตัว modal เอง
> ช่อง "เมื่อวานทำอะไร" ควรถูกเติมด้วย update เมื่อวานของคนนั้น และช่อง blocker
> ควรเติมด้วย work item ที่คนนั้นเกี่ยวข้องและสถานะยัง `blocked` — ข้อมูลนี้มีอยู่แล้ว
> ใน `topic.participants` + `topic.state` ไม่ต้องสร้างอะไรใหม่

### ชั้น B — Ambient (ไม่มีใครเริ่ม บอทอ่านเอง)

ชั้นนี้คือเหตุผลที่ Catchup ต่างจาก standup bot ทั่วไป **คนที่ไม่เคยพิมพ์คำสั่งเลย
ก็ยังถูกนับ** เพราะบอทอ่าน channel ผ่าน Events API (`message.channels`) แล้วส่งเข้า
pipeline เดียวกับ export วันนี้

- ข้อความปกติในแชนเนล → record → embed → เข้ากราฟ → กลายเป็นส่วนหนึ่งของ work item
- `message_changed` → re-embed (เล่ม 1 §8 พูดถึงข้อนี้แล้ว และ `embeddings.py`
  cache ด้วย content hash อยู่แล้ว จึงถูก invalidate เองโดยธรรมชาติ)
- reaction `:white_check_mark:` บนข้อความ → สัญญาณเสริมของ `resolves`
  (ยังไม่มีในรีโป — เป็น cue ชนิดใหม่ที่ไม่ใช่ข้อความ ควรเพิ่มเป็น signal ใน `signals.py`)

**ผลข้างเคียงที่ต้องยอมรับ:** พอบอทอ่านทุกอย่าง `/meow` จะกลายเป็นทางเลือก
ไม่ใช่หน้าที่ — และนั่นถูกแล้ว เป้าคือให้ทีมพิมพ์ *น้อยลง* ไม่ใช่มีพิธีเพิ่ม

### ชั้น C — Delivery (บอทเป็นคนพูด)

| เมื่อไร | ส่งอะไร | ทรง Slack | ทำไมทรงนี้ |
|---|---|---|---|
| ทันทีหลัง submit | เตือน copycat ถ้า sim ≥ 0.92 | **ephemeral** | ผิดพลาดได้ ต้องไม่มีใครเห็นนอกจากเจ้าตัว |
| 18:00 | Meow Mix digest | `chat.postMessage` ลง `#dev-daily` | ของสาธารณะ อ่านร่วมกัน |
| เมื่อ Déjà-Mew ทริกเกอร์ | เตือน lead | **DM เท่านั้น** | เรื่องระบบไม่ใช่เรื่องคน อย่าประจาน |
| เมื่อมีคนวาง URL ของเว็บลง Slack | unfurl เป็นการ์ด work item | `links:read` + `chat.unfurl` | สะพานสองทาง คนแชร์ลิงก์แล้วทีมเห็นสาระทันที |

`chat.unfurl` เป็นชิ้นเล็กที่คุ้มค่าที่สุดในลิสต์นี้ ทำครั้งเดียวแล้วทุกครั้งที่มีคน
แปะลิงก์เรื่องนี้ในแชท คนอื่นเห็น headline + สถานะ + อายุ ทันที ไม่ต้องกดออกจาก Slack

### สิ่งที่บังคับด้วยข้อจำกัดของ Slack ไม่ใช่ด้วยรสนิยม

- **ACK ภายใน 3 วินาที** — slash command ต้องตอบ 200 ทันที แล้วค่อยยิงผลผ่าน
  `response_url` (มีอายุ 30 นาที ใช้ได้ 5 ครั้ง) ปัญหาคือ `server.py` วันนี้
  ทำงานหนักทั้งหมด *ก่อน* ตอบ — โครงนั้นใช้กับ Slack ไม่ได้เลย ต้องมี queue คั่น
- **Socket Mode สำหรับ dev เท่านั้น** — prod ใช้ HTTP endpoint เพราะ Socket Mode
  ไม่มี URL ให้ Slack retry
- **manifest วันนี้ยังเป็น read-only** — `slack_app_manifest.json` มีแค่
  `channels:history`, `groups:history` ต้องเพิ่ม `commands`, `chat:write`,
  `im:write`, `files:read`, `links:read`, `reactions:read` และ `users:read`
  (ตัวสุดท้ายคือตัวที่แก้ปัญหา `U0DEMOUSER1` โผล่ในหน้าจอ — ดู §5)
- **ห้ามชื่อชนกับของ built-in** — เล่ม 1 §3 ตรวจแล้ว แต่ต้องตรวจอีกครั้งใน
  workspace จริงตอน install เพราะแอปอื่นจองไว้ได้

---

## 4. Web app — ห้าหน้า สี่หน้ามีอยู่แล้ว

`server.py` มีสี่หน้าและมี JSON endpoint คู่กันทุกหน้า (`/api/digest`,
`/api/blockers`, `/api/item/{key}`, `/api/search`, `POST /api/reindex`)
โครงนี้ถูกออกแบบไว้ให้เปลี่ยน frontend ได้โดยไม่แตะ logic — ใช้ต่อได้เลย

```
/                 Digest        หน้า 09:25 — blocked ก่อน แล้ว active แล้ว resolved
/blockers         Blockers      เฉพาะที่ติด ติดนานสุดขึ้นก่อน
/item/{key}       Work item     timeline ที่มีทิศทาง + ข้อความทั้งหมด
/search?q=        Ground a note  วาง note ประชุมทั้งย่อหน้า → หา Slack ที่อยู่เบื้องหลัง
/upload           Add a meeting  transcript → records → rebuild
/herding          Dashboard     (§5 — หน้าใหม่หน้าเดียวในเล่มนี้)
```

### หน้า 1 — Digest (หน้าที่วัดผลว่าโปรดักต์นี้รอดหรือไม่)

งานเดียวของหน้านี้: **คนเปิด 09:25 ห้านาทีก่อน standup ต้องรู้ว่าอะไรติดและติดเพราะอะไร**
อย่างอื่นรอง

```
┌──────────────────────────────────────────────────────────────┐
│ Catchup   ช่วง 3 วัน ▾      42 ข้อความ · สรุปโดย template     │
│                              สร้างเมื่อ 2026-08-15 20:44      │
├──────────────────────────────────────────────────────────────┤
│ ⚠ ติดอยู่   ติดมา 1.2 วัน                        slack 8 · mtg 2│
│ omega, usd, agentforce add — ติดอยู่ ยังไปต่อไม่ได้            │
│                                                              │
│ 10 ข้อความ · Nok, Tim, +2 คนที่ยังไม่ได้จับชื่อ                 │
│ ล่าสุด [Tim] ผมคุยกับทาง Omega เมื่อวาน เขาสนใจ Agentforce…    │
│                                                              │
│ ▸ หลักฐาน  blocked since 2026-08-14 16:31 — cue “ยังรอ”  →   │
│ ▸ ขั้นถัดไป ต้องมีคนไล่ให้ก่อน ถึงจะไปต่อได้                    │
│                                                              │
│ ┄ 2 ข้อความล่าสุด ┄                                          │
│ 2026-08-14 16:31  Tim   (meeting)  …ยังรอทีม Product ยืนยัน   │
│ 2026-08-14 16:30  Nok   (meeting)  ดีล Omega อยู่ stage…      │
└──────────────────────────────────────────────────────────────┘
```

การตัดสินใจที่ต้องเลือกตอนออกแบบจริง:

- **การ์ดสูงเท่าไร** — channel จริงให้ 5–15 items ต่อหน้าต่าง 3 วัน ถ้าการ์ดสูง
  350px จะเห็นสองใบต่อจอ ไม่พอ ผมเสนอ: blocked = การ์ดเต็ม, active = การ์ดครึ่ง,
  resolved = พับเป็นบรรทัดเดียว รวมกลุ่มท้ายหน้า
- **แถวหลักฐานต้องกดได้ทุกใบ** ลูกศร `→` เปิด drawer ที่โชว์ข้อความ `evidence_id`
  ตัวจริง พร้อมปุ่ม "เปิดใน Slack"
- **`next_step` ว่างได้** (`item.json` ของ resolved มี `next_step: ""`) แถวนั้น
  ต้องหายไปทั้งแถว ไม่ใช่โชว์หัวข้อว่าง

### หน้า 2 — Blockers

น้อยใบ กว้างใบ ประโยคหลักฐานกับข้อความที่บล็อกต้องเห็นพร้อมกันโดยไม่ต้องคลิก
`/api/blockers` คืน `{"blocked": [...]}` เรียง "ติดนานสุดก่อน" มาให้แล้ว

**หน้านี้ว่างบ่อย และว่างคือข่าวดี** — ต้องออกแบบเป็นข่าวดี ไม่ใช่ error state
เล่ม 1 ให้ภาษาไว้แล้ว: `purr` 😻 ใช้ที่นี่ได้ตรงที่สุด (ไม่ใช่ที่หน้าอื่น)

### หน้า 3 — Work item detail

สองส่วน และส่วนแรกคือส่วนที่ไม่มีเครื่องมืออื่นทำ:

```
timeline (จาก digest.timeline())
─────────────────────────────────────────────────────────────
2026-08-14 16:30   resolves        cue “เสร็จแล้ว”
  จาก [U0DEMOUSER1] Hey team, we've got a user reporting…
   └→ [Ake] ผม debug แล้วครับ เจอ bug ใน Profile module…
        · เหตุการณ์นี้ยัง answers อีก 4 ข้อความ (พับไว้)
```

ทิศทางต้องอ่านออกจากรูป ไม่ใช่จากคำ — `from` คือข้อความที่เกิดก่อน `to` คือข้อความ
ที่มาตอบ/ปิด/ตามต่อ ทิศมาจาก timestamp (`relations.py` — "ในแชท ข้อความที่มาหลัง
คือข้อความที่ตอบ") ปกติมี 1–6 แถว และ `also_answers` ใช้พับส่วนซ้ำ

ส่วนที่สอง: ข้อความทั้งหมดในไอเทม เรียงเวลา มี source + author
ต้องเผื่อกรณี **หนึ่งไอเทม 40+ ข้อความ** ให้ยังเดินอ่านได้

### หน้า 4 — Ground a note

ช่อง input ต้องใหญ่ เพราะคนวาง *ทั้งย่อหน้า* ของ note ประชุม ไม่ใช่คีย์เวิร์ด
ผลลัพธ์แต่ละชิ้นมี `why` (คะแนนแยก stage: `dense`, `bm25`, `cross`) และ `terms`
(คำที่แมตช์ตรงตัว) — สองอันนี้คือ "ทำไมจึงเจอ" และเป็นของที่คนเชื่อถือเครื่องมือเพราะมัน

> **`score` ≈ 0.032** ไม่ใช่เปอร์เซ็นต์ ห้ามเรนเดอร์เป็น progress bar หรือ %
> ถ้าจะโชว์ให้โชว์เป็นอันดับ (`rank`) กับ `terms` ที่ไฮไลต์ในตัวข้อความ
> ค่า `dense: 1.0` ที่เห็นใน fixture คือค่า normalize ในชุดผลลัพธ์นั้น ไม่ใช่ "ตรง 100%"

### หน้า 5 — Add a meeting

`POST /upload` วันนี้ทำสามอย่างต่อกัน: parse → merge เข้า `messages.json` →
`state.rebuild()` แล้ว redirect กลับหน้าแรก การ rebuild กิน 5–60 วินาที และช่วงนั้น
API อื่นควรตอบ 503

**ต้องออกแบบการรออย่างซื่อสัตย์** — ไม่ใช่ spinner ลอย ๆ แต่บอกว่ากำลังทำอะไร
(เข้ารหัสข้อความใหม่ 37 บรรทัด → จัดกลุ่ม → หาความสัมพันธ์) เพราะ `embeddings.py`
cache ด้วย content hash เฉพาะบรรทัดใหม่ที่ถูก encode จริง เวลาที่ใช้จึงพอทำนายได้
จากจำนวนบรรทัด

---

## 5. Dashboard (`/herding`) — คิวงาน ไม่ใช่กระดานคะแนน

หน้าเดียวที่ยังไม่มีในรีโป และเป็นหน้าที่พลาดง่ายที่สุด เพราะแรงกดดันจะพาไปทาง
"เอาตัวเลขคนมาเรียง" ทุกครั้ง

**กฎของหน้านี้: ทุกแถวต้องจบด้วยการลงมือได้ ถ้าแถวไหนอ่านแล้วทำอะไรไม่ได้ ตัดออก**

| แถว | ที่มาของข้อมูล | ปุ่มที่จบแถว |
|---|---|---|
| ติดเกิน 2 วัน (นานสุดก่อน) | `state == "blocked"` + `age_days > 2` | เปิด thread · นัดคุย · เขียนใน thread ว่ากำลังตาม |
| Déjà-Mew ที่ยัง active | blocker ที่ sim ≥ 0.85 กับ blocker เดิม และโผล่ ≥ 3 วันในรอบ 14 วัน | ดูรอบก่อนหน้า · escalate |
| `ongoing` เกิน 4 วันโดย similarity สูง | tag จาก policy 0.75–0.92 (เล่ม 1 §5) | ถามเจ้าของว่า under-estimate หรือติดเงียบ |
| ไอเทมที่ยังไม่มีใครในทีมแตะใน 3 วัน | `last` เก่ากว่าหน้าต่าง แต่ state ยัง `blocked` | ปิดทิ้ง หรือรับไปทำ |
| เธรดที่ยังจับคู่ ticket/คลัสเตอร์ไม่ได้ | กอง unassigned จาก §2 | ผูกเข้า work item · เปิด ticket ใหม่ · ทิ้ง |

แถวสุดท้ายเป็นแถวที่คนอยากซ่อนที่สุดและต้องไม่ซ่อน — มันคือมาตรวัดว่าตัวผูกข้อความ
ทำงานดีแค่ไหน ถ้ากองนี้โตขึ้นเรื่อย ๆ แปลว่า threshold ผิด ไม่ใช่แปลว่าทีมพิมพ์ไม่ดี

### ตัวเลขสี่ตัวบนสุด — และตัวที่ห้ามใส่

**ใส่ได้:** จำนวน blocked ตอนนี้ · blocked ที่นานสุดกี่วัน · จำนวน Déjà-Mew active ·
อัตราการส่ง update **ระดับทีม** (ตัวเดียว ไม่แยกคน — เล่ม 1 §9 ข้อ 4)

**ห้ามใส่ และเหตุผล:**

| ห้าม | เพราะ |
|---|---|
| team velocity / story point | API ไม่คืน และรีโปนี้ไม่มีข้อมูลนั้นเลย ต้องกุ |
| leaderboard รายคน | เล่ม 1 §9 ข้อ 4 — ยกเว้นเรื่องชวนกันชม (`/catnip`) |
| donut / gauge / sparkline | ไม่มี metric ต่อเนื่องพอที่จะพลอต; เล่ม `DESIGN_BRIEF` สั่งห้ามไว้ตรง ๆ |
| "AI confidence 87%" | `score` ที่มีคือ ~0.03 การแปลงเป็น % คือการโกหก |

### เรื่อง `U0DEMOUSER1`

`participants` ในข้อมูลจริงปนกันสองแบบเสมอ: `["Nok", "Tim", "U0DEMOUSER2", "U0DEMOUSER1"]`
ชื่ออ่านได้มาจาก transcript ประชุม (มีชื่อคนพูด) ส่วน id ดิบมาจาก Slack export
ที่ไม่ได้เรียก `users.info`

สองทางที่ทำได้ และควรทำทั้งคู่:

1. **แก้ที่ราก** — เพิ่ม scope `users:read` แล้ว resolve id → display name ตอน ingest
   (แก้ครั้งเดียว ปัญหาหายจากทุกผิว)
2. **ยังต้องออกแบบเผื่อไว้** — เพราะ resolve พลาดได้ (คนออกจาก workspace, guest)
   UI ต้องแสดง id ดิบอย่างมีศักดิ์ศรี: `+2 คนที่ยังไม่ได้จับชื่อ` พร้อม tooltip ที่โชว์ id
   **ห้ามสร้าง avatar ปลอมหรืออักษรย่อ "U0"** ให้มัน

---

## 6. สะพาน Slack ↔ Web

### id ที่มีอยู่แปลงกลับเป็น permalink ได้

id ในรีโปเป็นสองทรง และทรงแรกมีทุกอย่างที่ต้องใช้อยู่แล้ว:

```
msg_C0DEMOCHAN1_1786630933.931999
    └ channel ┘ └────── ts ──────┘
    →  https://<workspace>.slack.com/archives/C0DEMOCHAN1/p1786630933931999
mtg_20260814-0930-daily-standup_1786699865.000000
    └────── meeting slug ─────┘
    →  ไม่มี permalink — ลิงก์ไปหน้า transcript ในเว็บแทน
```

กติกา: ทุกที่ที่โชว์ข้อความจาก `source: "slack"` ต้องลิงก์ออก Slack ได้
ทุกที่ที่โชว์ `source: "meeting"` ต้องลิงก์ไปตำแหน่งในเว็บที่มี transcript รอบนั้น
**ไอคอนต้นทางไม่ใช่ของประดับ มันคือคำสัญญาว่าคลิกแล้วไปไหน**

### ใครเห็นอะไร — ข้อที่ต้องตัดสินก่อนเขียนโค้ดบรรทัดแรก

เว็บนี้อ่านทั้ง channel ของทีม ถ้า auth ผิดตั้งแต่ต้น แก้ทีหลังคือเขียนใหม่:

- **Sign in with Slack** เท่านั้น ไม่มี user/password ของตัวเอง
- คนเห็นได้เฉพาะ work item ที่ประกอบจาก channel ที่**ตัวเองอยู่** — เช็คด้วย
  `conversations.members` ตอน login แล้ว cache ไว้ ไม่ใช่เช็คตอน query
- transcript ประชุมคือส่วนที่อ่อนไหวที่สุด (คนพูดสิ่งที่ไม่ได้ตั้งใจเขียน)
  ต้องผูก ACL กับ channel ที่อัปโหลดเข้าไป ไม่ใช่ workspace-wide
- `/api/*` ต้องมี auth เหมือนหน้า HTML — วันนี้ยังไม่มีทั้งคู่ ยอมรับได้ตอน PoC
  ยอมรับไม่ได้วันที่มีคนที่สองใช้

---

## 7. สถาปัตยกรรม — จาก PoC วันนี้ ไปของที่รับ Slack ได้

```
Slack   slash · event · reaction · file_shared ─┐
YouTrack  webhook (issue/comment updated) ──────┼──►  FastAPI  (ACK ≤ 3 วิ ทุกกรณี)
transcript  .vtt/.srt/.txt/.json ───────────────┘         │
                                                          ▼
                                                Task queue (arq)
                                                          │
                                                    ┌─────┴─────┐
                                                    ▼           │
                                            ingest + embed      │
                                            embeddings.py       │
                                            meetings.py         │
                                                    │           │
                                                    ▼           │
                                              ★ linker  ◄── human override
                                              ticket key → thread → คลัสเตอร์
                                              (§2 · ยังไม่มีในรีโป)
                                                    │
                              ┌─────────────────────┼─────────────────────┐
                              ▼                     ▼                     ▼
                        relate + cluster       state engine          summarize
                        relations.py           digest.py             summarize.py
                        graph.py               (cue + timing)        template | claude
                              │                     │                     │
                              └────────► Postgres + pgvector ◄────────────┘
                                  records · embeddings · work_items · relations
                                  · links (+ overrides) · decisions
                                                    │
              ┌─────────────────────────────────────┴──────────────────────────┐
              ▼                                                                ▼
      Catchup (ทีม dev)                                              ตาม (PO) — §13
      web: / /blockers /item /search /herding                        PO board · drift
      slack: digest 18:00 · ephemeral · unfurl · DM                  YouTrack write-back
```

### โมดูลที่มี ใช้ต่อได้แค่ไหน

| ไฟล์ | สถานะ | ต้องทำอะไร |
|---|---|---|
| `embeddings.py` | **ใช้ได้เลย** | cache เป็น content hash อยู่แล้ว → incremental ได้โดยธรรมชาติ |
| `retrieve.py` `fusion.py` `rerank.py` `lexical.py` `signals.py` | **ใช้ได้เลย** | เปลี่ยนแหล่ง records จากไฟล์เป็น DB |
| `relations.py` `graph.py` | ใช้ได้ ต้องเพิ่ม | รับ reaction เป็น signal; รัน incremental ต่อคลัสเตอร์ ไม่ใช่ทั้ง corpus |
| `digest.py` `summarize.py` | ใช้ได้เลย | มี `unverified` flag แล้ว — UI ต้องเคารพมัน |
| `meetings.py` | ใช้ได้เลย | รับ `.vtt/.srt/.txt/.json` ครบแล้ว รวมบรรทัดคนพูดเดิมให้แล้ว |
| `server.py` | **ต้องแยกสองส่วน** | `State.rebuild()` ทำทุกอย่างตอน startup — ใช้กับหลายทีมไม่ได้ ต้องย้ายเข้า worker |
| `slack_app_manifest.json` | ต้องเขียนใหม่ | วันนี้ read-only ล้วน (§3) |
| **linker** | **ไม่มี ต้องเขียนใหม่** | ชิ้นเดียวที่ไม่มีเค้าอยู่เลย และเป็นชิ้นที่ตัดสินว่าทั้งระบบน่าเชื่อหรือไม่ (§2, §10) |
| **YouTrack client** | ไม่มี ต้องเขียนใหม่ | Catchup ใช้แค่ **อ่าน** (issue id, status, วันแก้ description ล่าสุด) · การ**เขียนกลับ**เป็นของ *ตาม* (§13) |

### สามข้อที่จะเจ็บถ้าไม่คิดวันนี้

1. **`state.rebuild()` คือ single-tenant โดยกำเนิด** — อัปโหลด transcript หนึ่งไฟล์
   แล้ว index ทั้งก้อนถูกสร้างใหม่ พอมีสองทีมในระบบเดียว ทีม B รอเพราะทีม A
   อัปโหลด นี่คือชิ้นที่ต้องรื้อก่อนชิ้นอื่น
   → **§11 ตัดสินแล้วว่าเป็นหลายทีม ข้อนี้จึงไม่ใช่ความเสี่ยงแล้ว แต่เป็นงานชิ้นแรก**
   และต้องแยกตอน *build* ไม่ใช่ตอน query ไม่งั้นคลัสเตอร์จะปนกันข้ามทีม
2. **pgvector พอสำหรับสเกลนี้จริง** — เล่ม 1 คำนวณไว้ 20 คน × 250 วัน ≈ 5,000
   vector/ปี ต่อทีม ไม่ต้องมี vector DB แยก และการมี Postgres ก้อนเดียวทำให้
   join `topics × relations × records` ได้ในคิวรีเดียว ซึ่งคือทุกหน้าของเว็บ
3. **โมเดล embedding ต้อง multilingual** — รีโปนี้เทียบมาแล้ว 5 ตัว
   (`bge-m3`, `multilingual-e5-base/large`, `paraphrase-multilingual-*`)
   ผลอยู่ใน `output/model_comparison*.html` ให้ตัดสินจากไฟล์นั้น ไม่ใช่จาก benchmark ภาษาอังกฤษ

---

## 8. ภาษาออกแบบ — ใช้ร่วมกันทั้งสามผิว

### ไทยเป็นเนื้อหาหลัก ไม่ใช่ภาษาที่สอง

- font stack ต้องมีหน้าไทยจริง: `"IBM Plex Sans Thai"`, `"Noto Sans Thai"`,
  `"Sarabun"`, `"Sukhumvit Set"` — Latin-only จะ fallback เงียบ ๆ แล้วดูพัง
- ไทยไม่เว้นวรรคระหว่างคำ → บรรทัดตัดที่ไหนก็ได้ **ห้ามพึ่ง `text-overflow: ellipsis`
  ที่ขอบคำ และห้ามตัดด้วยจำนวนตัวอักษรคงที่**
- สระและวรรณยุกต์ซ้อนบนล่าง → `line-height: 1.6–1.75` มากกว่าที่ใช้กับ Latin
- **ห้าม `letter-spacing` กับไทย** (สระหลุดจากพยัญชนะ) และ **ห้าม uppercase**
- ไทยกับอังกฤษอยู่ประโยคเดียวกันตลอด ("รอ API sorting อยู่") → optical size ต้องเท่ากัน

### ของที่คำนวณได้ กับของที่ AI เขียน ต้องดูต่างกัน

นี่คือแกนของแบรนด์ทั้งอัน ไม่ใช่รายละเอียด:

| ประเภท | ตัวอย่าง field | น้ำหนักทางสายตา |
|---|---|---|
| คำนวณได้ | `state`, `evidence`, `participants`, `sources`, `age_days`, timeline | หนัก ชัด เป็นข้อเท็จจริง |
| AI เขียน | `summary.headline`, `summary.detail`, `summary.next_step` | เบากว่า มีป้ายว่ามาจากไหน |
| AI เขียนแต่ตรวจไม่ผ่าน | `summary.unverified == true` | มีคำเตือน แต่**ยังโชว์ของที่คำนวณได้ตามปกติ** |

`summary.backend` บอกว่า `"template"` หรือ `"claude"` — โชว์มันที่หัวหน้า digest
ทีมควรรู้ว่ากำลังอ่านของที่เครื่องต่อประโยคหรือของที่โมเดลเขียน

**ห้ามตกแต่งทั้งแอปเป็น "✨ AI-powered"** ขอบเรืองแสง แอนิเมชันพิมพ์ทีละตัว
ม่วง gradient บนดำ — ของพวกนี้ทำให้ *ข้อเท็จจริงที่คำนวณได้* ดูเหมือนของที่กุขึ้น
ซึ่งกลับหัวกลับหางกับสิ่งที่โปรดักต์นี้ขาย

### เข้าถึงได้ และไม่โกหก

- สถานะไม่พึ่งสีเดียว ๆ (ไอคอน + คำ ไม่ใช่จุดสี)
- ตัวอักษรบนพื้นสีคอนทราสต์ ≥ 4.5:1
- เวลาเป็นเวลาจริง (`2026-08-14 16:31`) ไม่ใช่ "2 วันก่อน" ลอย ๆ เพราะคนอ่าน
  กำลังเทียบกับความจำของตัวเองว่าสัปดาห์นี้เกิดอะไร
- light + dark เลือกทั้งสองอย่างตั้งใจ ผ่าน CSS custom properties
- ใช้ได้ที่ 390px และ 1440px — digest คือหน้าที่ mobile สำคัญที่สุด

---

## 9. Guardrails เฉพาะของผิวใหม่

ต่อจากเล่ม 1 §9 ซึ่งยังใช้ทั้งหมด นี่คือข้อที่เกิดขึ้นเพราะมีเว็บกับ dashboard เพิ่ม:

1. **dashboard ไม่มีหน้าที่เรียงคน** ถ้ามีใครขอ "อยากดูว่าใครส่ง update ครบ" คำตอบคือ
   ตัวเลขระดับทีมหนึ่งตัว ไม่ใช่ตาราง 20 แถว
2. **หน้า `/item` ต้องเปิดได้เฉพาะคนที่อยู่ใน channel ต้นทาง** — work item หนึ่งใบ
   คร่อมหลาย channel ได้ ถ้าคร่อม ให้เห็นเฉพาะส่วนที่ตัวเองมีสิทธิ์ และบอกตรง ๆ ว่า
   "มีอีก 3 ข้อความจาก channel ที่คุณไม่ได้อยู่"
3. **retention บังคับที่ระดับ storage ไม่ใช่ที่ UI** — raw text 90 วัน, embedding
   12 เดือน (เล่ม 1 §9 ข้อ 5) พร้อมปุ่ม "ลบข้อมูลของฉัน" ใน App Home ที่ทำงานจริง
   และเว็บต้องเลิกโชว์ทันทีที่ลบ ไม่ใช่รอ rebuild รอบถัดไป
4. **transcript ต้องมี "ใครอัปโหลด เมื่อไร"** อยู่บนหน้าไอเทมทุกครั้ง เพราะมันคือ
   ข้อมูลที่คนไม่ได้ตั้งใจให้ถูกค้นหา ต้องมีคนรับผิดชอบเสมอ
5. **ไม่มีหน้า settings ในบอท** — ค่าตั้งทั้งหมดอยู่ App Home + เว็บ
   (เล่ม 1 §3 ตัดสินไว้แล้ว เอกสารนี้ไม่รื้อ)
6. **เพดานการทวง: 1 ครั้งต่อคนต่อวัน** ทุกช่องทางรวมกัน และมีปุ่ม snooze ทุกที่
   ค่า default ของบอทคือ**เงียบ** — ถ้าวันหนึ่งมีทั้ง copycat, stray, และ Déjà-Mew
   ยิงหาคนเดียวกัน ต้องเลือกส่งอันเดียวและทิ้งที่เหลือ ไม่ใช่ส่งครบ
7. **PDPA และขอบเขตการอ่าน** — อ่านเฉพาะ channel ใน allowlist ที่ทีมรู้ตัว
   **ไม่อ่าน DM ระหว่างคนสองคนเด็ดขาด** transcript เอาเฉพาะประชุมที่ผู้เข้าร่วมถูกบอกแล้ว
   และต้องเขียน retention ลงเอกสารก่อนเปิดใช้ ไม่ใช่หลังจากมีคนถาม
8. **บอทไม่เขียนอะไรลง ticket เองโดยไม่มีคนกด** — Catchup อ่าน YouTrack เท่านั้น
   ส่วนการเขียนกลับ (ของ *ตาม*) ต้องมีคนกดยืนยันทุกครั้งพร้อมเห็น diff ก่อน
9. **ทุกอย่างที่แก้ได้ ต้องแก้ได้โดยทุกคนในทีม** ไม่ใช่แค่ lead หรือ PO —
   ถ้าการแก้ link ผูกอยู่กับคนคนเดียว ระบบนี้ก็แค่สร้างงานธุรการชิ้นใหม่ให้เขา

---

## 10. แผนสร้าง — map กับรีโปนี้

| Phase | ผิวที่เปิด | ของที่ต้องเขียนใหม่จริง | เกณฑ์ผ่านไป Phase ถัดไป |
|---|---|---|---|
| **0 · เงียบ** (2–3 สัปดาห์) | Web อ่านอย่างเดียว จาก export ที่มี | auth (Sign in with Slack) + ACL ระดับ channel + **linker** (§2) | **สองเกณฑ์ ต้องผ่านทั้งคู่** — (ก) ความแม่นของการผูกข้อความ ≥ 90% บนตัวอย่าง 200 ข้อความที่ label ด้วยมือ (ข) ทีม 1 ทีมเปิดหน้า digest เองก่อน standup 5 วันติด **โดยไม่มีใครเตือน** |
| **1 · Core** (3–4 สัปดาห์) | + Slack ชั้น A และ C | queue, `/slack/command`, `views.*`, digest 18:00, ephemeral copycat | ephemeral copycat แม่นพอที่คนไม่กด "ยืนยันส่งเลย" เกินครึ่ง |
| **2 · Ambient + Insight** (หลังมีคนใช้จริง) | + Slack ชั้น B, `/herding`, unfurl | Events API, incremental cluster, Déjà-Mew, Pounce Link | lead ใช้ `/herding` แทนการไถ่ถาม |
| **3 · ท่ารัก** | Tuna points, โปสเตอร์แมวหาย, `/catnip` | gamification (opt-in ระดับ workspace) | core loop อยู่ได้เองก่อน ค่อยใส่ |

**เกณฑ์ 90% เป็นเกณฑ์ที่ห้ามข้าม และห้ามผ่านด้วยวันที่** — ถ้าตัวผูกข้อความผูกผิด
ทุกฟีเจอร์ที่อยู่ข้างหลังมันกลายเป็นคำโกหกที่ดูน่าเชื่อ และทีมจะเลิกอ่านภายในสัปดาห์เดียว
label 200 ข้อความด้วยมือใช้เวลาราวสองชั่วโมง และเป็นสองชั่วโมงที่คุ้มที่สุดในแผนทั้งอัน
เพราะมันได้ทั้งเกณฑ์วัดและชุด eval ที่ใช้จูน threshold ต่อ — รีโปนี้มีที่วางให้อยู่แล้ว
(`data/eval_queries.*.json`, `evaluate.py`)

**Phase 0 มีค่ามากกว่าที่หน้าตามัน** — เพราะมันวัดสิ่งเดียวที่ทำนายทุกอย่าง:
*คนเปิดอ่านเองไหมถ้าไม่มีใครบอกให้เปิด* ถ้าคำตอบคือไม่ การเพิ่ม 15 slash command
ก็ไม่ช่วย และ threshold ทั้งหมด (0.92 / 0.85 / 0.75) ควรถูกวัดจาก distribution
จริงของทีมใน Phase 0 ไม่ใช่ใช้ค่าที่เขียนในเอกสารนี้ตรง ๆ

---

## 11. คำถามที่เกิดจากผิวใหม่ — ตัดสินแล้ว 3 ข้อ

ต่อจากเล่ม 1 §12 · ตัดสินเมื่อ 17 ส.ค. 2026

### ตัดสินแล้ว

#### ข้อ 1 · tenancy = **หลายทีม ใน workspace เดียว**

ไม่ใช่ SaaS หลาย workspace และไม่ใช่ทีมเดียว สิ่งที่ตามมาทั้งหมด:

- **tenant = "ทีม" = ชุด channel** ไม่ใช่ workspace → ใช้ bot token ชุดเดียว
  ยังไม่ต้องทำ OAuth หลาย workspace, token rotation, billing, admin console
- **corpus แยกต่อทีม ตอน build ไม่ใช่ตอน query** — ถ้า filter แค่ตอน query
  `graph.py` จะรวมข้อความสองทีมเข้าเป็นคลัสเตอร์เดียวและ work item จะปนกัน
  นี่คือความต่างที่ทำให้ข้อนี้เป็นงานสถาปัตยกรรม ไม่ใช่งาน UI
- `State` กลายเป็น `dict[team_id, TeamState]` และ `rebuild(team_id)` →
  **§7 ข้อ 1 เลื่อนจาก "ข้อควรระวัง" เป็นงานที่ต้องทำก่อนหน้าใหม่ทุกหน้า**
- digest ยิง **per-team** (ตอบข้อ 4 ไปครึ่งข้อ)
- ACL เดิมยังใช้ (เห็นเฉพาะ channel ที่ตัวเองอยู่) แต่ "ทีม" เป็น scope ที่ใหญ่กว่า
  channel → เว็บต้องมีตัวสลับทีมสำหรับคนที่อยู่หลายทีม และ `/herding` เป็นของ **ต่อทีม**

#### ข้อ 3 · **local ก่อน แต่เขียนให้ขึ้น cloud ได้ทีหลังโดยไม่รื้อ**

ค่า default วันนี้คือทุกอย่างรันในเครื่อง — embedding local (`bge-m3` /
`multilingual-e5` ที่รีโปนี้รันได้อยู่แล้ว) และ summarizer เป็น `template`

**ข้อนี้ตอบข้อ 6 ไปด้วย:** default = `template` เพราะไม่ต้องส่งข้อมูลออก ·
`claude` เป็น **opt-in ต่อทีม** · และหน้า digest ต้องโชว์ `summary.backend`
เสมอ (§8) เพื่อให้คนรู้ว่ากำลังอ่านของแบบไหน

"cloud-ready" ต้องแปลเป็นของที่ทำได้ ไม่ใช่คำสัญญา — สี่ข้อนี้ทำวันนี้ ที่เหลือรอได้:

| ต้องทำวันนี้ | เพราะถ้าไม่ทำ ตอนขึ้น cloud ต้องรื้อ |
|---|---|
| ค่าตั้งทุกตัวมาจาก env/config ไม่ใช่ค่าคงที่ในโค้ด | path, channel id, ชื่อโมเดล วันนี้กระจายอยู่หลายไฟล์ |
| แหล่งเก็บ records อยู่หลัง interface เดียว | `meetings.merge_into()` เขียน `messages.json` ลงดิสก์ตรง ๆ ของจริงคือ Postgres |
| embedder + summarizer อยู่หลัง interface ที่ swap ได้ | มีเค้าอยู่แล้ว (`embeddings.set_model()`, `summarize.backend_name()`) แค่ทำให้เป็นสัญญาที่ชัด |
| ไม่มี state สำคัญอยู่ในหน่วยความจำของโปรเซสเดียว | `State` วันนี้ *คือ* state ทั้งระบบ |

**ยังไม่ต้องทำ:** multi-region, secret manager, autoscaling, queue แบบ managed —
`arq` + Postgres ในเครื่องพอสำหรับสเกลใน §7 ข้อ 2

### ยังเปิดอยู่ (เรียงตามความเร่ง)

1. **transcript ประชุมใครอัปโหลด** (ข้อ 2 เดิม) — ถ้าเป็นคนเดียว คนนั้นจะเลิกทำ
   ในสัปดาห์ที่สาม ถ้าจะให้ยั่งยืนต้องต่อ Zoom/Meet webhook ตรง แล้ว `/upload`
   เป็นทางสำรอง · เร่งเพราะ transcript คือครึ่งหนึ่งของสิ่งที่ทำให้ digest มีค่า
2. **หน้าต่างเริ่มต้นควรเป็น 3 วันจริงไหม** (ข้อ 5 เดิม) — `DEFAULT_WINDOW_DAYS`
   วันนี้เป็นค่าเดียวสำหรับทุกคน แต่พอ tenancy เป็นทีม มันควรเป็นค่าต่อทีม
   และผูกกับความถี่ของ standup ในเทมเพลต (§12)
3. **timezone** (ครึ่งที่เหลือของข้อ 4) — ถ้าทุกทีมอยู่ไทยหมด ข้อนี้ปิดได้ทันที
   ถ้าไม่ ให้ตามเทมเพลต: ถามตาม timezone ของแต่ละคน แต่ digest ยิงตาม timezone ของทีม

---

## 12. Standup template — สิ่งที่ Geekbot ทำ และส่วนที่ Catchup ต่อจากนั้น

Geekbot มี template ชื่อ *Daily Standup* ที่คนคุ้นอยู่แล้ว
([geekbot.com/templates/daily-standup](https://geekbot.com/templates/daily-standup/))
ของมันมีสี่คำถาม:

1. What have you done since `{last_report_date}`?
2. What will you do today?
3. Anything blocking your progress?
4. How do you feel today?

ตั้งค่าได้สี่อย่าง — ความถี่/เวลา (default จันทร์–ศุกร์ 10:00 **ตาม timezone ของแต่ละคน**),
ตัวคำถาม, ผู้เข้าร่วม (เลือกเป็นคนหรือทั้ง channel), และ channel ปลายทาง (public/private)
ขั้นตอนของผู้ใช้มีสาม step: *add this template → customize it → hit publish*

> **ข้อสังเกตที่สำคัญที่สุด:** modal 4 ช่องของ `/meowmeow` ในเล่ม 1
> (เมื่อวาน / วันนี้ / blocker / mood) คือคำถามชุดเดียวกันเป๊ะ
> ดังนั้นนี่ไม่ใช่ฟีเจอร์ใหม่ที่ต้องคิด — เป็นการยอมรับว่าชุดคำถามนี้เป็นมาตรฐานไปแล้ว
> และสิ่งที่ต้องเพิ่มคือ **ทำให้มันเป็นเทมเพลตที่ตั้งค่าได้ ไม่ใช่ค่าคงที่ในโค้ด**

### สิ่งที่ควรลอกตรง ๆ เพราะคนคาดหวังอยู่แล้ว

| ของ Geekbot | Catchup ทำอย่างไร |
|---|---|
| ชุดคำถาม default 4 ข้อ | มาเป็นเทมเพลต `daily-standup` ติดมากับระบบ แก้ได้ ลบข้อได้ เพิ่มข้อได้ |
| `{last_report_date}` แทนค่าอัตโนมัติในคำถาม | เรามีข้อมูลนี้อยู่แล้ว — "ทำอะไรไปแล้วตั้งแต่ **14 ส.ค. 18:02**" และ**ดีกว่านั้นคือ pre-fill คำตอบ** ไม่ใช่แค่ใส่วันที่ในคำถาม (§3) |
| ถามตาม timezone ของแต่ละคน | ปิดข้อ timezone ที่ยังเปิดอยู่ — เก็บ tz ต่อคนจาก `users.info` ไม่ต้องให้ใครตั้ง |
| เลือกผู้เข้าร่วมเป็นคนหรือทั้ง channel | ผูกกับ tenancy ที่เพิ่งตัดสิน: participant set = ทีม |
| รายงานโพสต์ลง channel ที่เลือก | Meow Mix digest มีอยู่แล้ว (§3 ชั้น C) |
| add → customize → publish สาม step | เป็น flow ตั้งค่าใน **เว็บ / App Home** ไม่ใช่ slash command (§9 ข้อ 5) |

### สิ่งที่ไม่ลอก — และนี่คือเส้นแบ่งของโปรดักต์

Geekbot จบที่ "เก็บคำตอบแล้วโพสต์ต่อ" รายงานของมันคือ **คำตอบของคน n คน เรียงกัน**
Catchup ถือว่าคำตอบเป็น*วัตถุดิบ* ไม่ใช่ผลลัพธ์:

| | Geekbot | Catchup |
|---|---|---|
| หน่วยของรายงาน | คน | **work item** (§2) |
| สถานะของงาน | คนพิมพ์เองว่าติด | **คำนวณจาก typed relation ล่าสุด** + หลักฐาน (`evidence_id`) |
| blocker ที่ซ้ำทุกวัน | ไม่รู้ ต้องมีคนสังเกตเอง | Déjà-Mew — sim ≥ 0.85 และโผล่ ≥ 3 วันในรอบ 14 วัน |
| คนที่ไม่ตอบแบบสอบถาม | รายงานว่างช่องนั้น | ชั้น ambient ยังนับจากข้อความจริงใน channel (§3) |
| ประชุม | ไม่เกี่ยว | transcript เข้า corpus เดียวกัน คลัสเตอร์เดียวกัน |
| ที่มาของทุกประโยคในรายงาน | คำตอบดิบของคน | ทุกบรรทัดกลับไปหาข้อความต้นทางได้ในหนึ่งคลิก (§1, §6) |

> ถ้า Catchup ทำแค่ระดับ Geekbot ก็ไม่มีเหตุผลให้สร้าง — เพราะของนั้นมีอยู่แล้ว
> ราคาถูกกว่า และเสถียรกว่าของที่เราจะเขียนเสร็จเดือนหน้า
> **เทมเพลตคือทางเข้าที่คนคุ้น ส่วนที่ขายคือสิ่งที่เกิดขึ้นหลังกดส่ง**

### รูปร่างของเทมเพลต

ทั้งชุดคำถามและตารางเวลาเป็นค่าต่อทีม เก็บเป็นไฟล์เดียวที่อ่านออกด้วยตาเปล่า:

```yaml
template: daily-standup
team: rever-cms
schedule:
  days: [mon, tue, wed, thu, fri]
  ask_at: "09:30"          # ถามตาม timezone ของแต่ละคน
  digest_at: "18:00"       # digest ตาม timezone ของทีม
  window_days: 3           # ต่อทีม ไม่ใช่ค่ากลางของระบบ
post_to: "#dev-daily"
questions:
  - id: done
    text: "ทำอะไรไปแล้วตั้งแต่ {last_report}"
    prefill: last_update       # ← ของที่ Geekbot ไม่มี
    maps_to: update
  - id: today
    text: "วันนี้จะทำอะไร"
    maps_to: update
  - id: blocker
    text: "มีอะไรติดอยู่ไหม"
    prefill: open_blockers     # work item ของคนนี้ที่ยัง blocked
    maps_to: blocked_by        # ← ป้อนตรงเข้า relations.py
  - id: mood
    type: emoji
    maps_to: cat_state         # สถานะของ *คน* ไม่ใช่ของงาน (§2)
```

`maps_to` คือช่องที่สำคัญที่สุดในไฟล์นี้ มันบอกว่าคำตอบแต่ละช่องจะกลายเป็น**อะไร**
ในระบบ ไม่ใช่แค่ข้อความที่เอาไปต่อกัน — ช่อง blocker กลายเป็นสัญญาณของ `blocked_by`
ช่อง mood กลายเป็น cat state ของคน และช่อง done/today เข้า corpus เป็น update ปกติ
**ถ้าเพิ่มคำถามใหม่แล้วไม่มี `maps_to` ให้ ต้องถือว่าคำถามนั้นยังไม่ควรมี**

เทมเพลตที่ควรมีตั้งแต่วันแรก มีสามอันพอ — `daily-standup` (ข้างบน),
`blocker-only` สำหรับทีมที่ไม่อยากถูกถามทุกวัน, และ `weekly-retro`
ที่ถามสัปดาห์ละครั้งด้วย `window_days: 7`

---

## 13. เส้นแบ่งกับ *ตาม* (Tam) — สองโปรดักต์ backend เดียว

ตัดสิน 17 ส.ค. 2026: **แยกเป็นสองโปรดักต์ ไม่ยุบรวม** เพราะผู้ใช้ต่างกันจริง
Catchup พูดกับทีม dev (คนที่ *ทำ* งาน) · *ตาม* พูดกับ PO (คนที่ *ตาม* งาน)
เอกสารของตามอยู่ที่ `tam-proposal.md`

เหตุผลที่แยกดีกว่ายุบ: หน้าจอที่ตอบความเจ็บของ PO ("ลืมว่า dev คนนี้ค้างอะไร",
"requirement เปลี่ยนแต่ ticket ไม่อัปเดต") กับหน้าจอที่ทีม dev จะเปิดเอง
มีเกณฑ์ความสำเร็จ**ตรงข้ามกัน** — ของ PO ยิ่งครบยิ่งดี ของ dev ยิ่งน้อยยิ่งดี
ถ้ายุบเป็นตัวเดียว หน้าจอจะเอียงไปทาง PO แน่นอน แล้ว dev จะเลิกเปิด
และเมื่อ dev เลิกเปิด ข้อมูลก็หยุดไหล แล้วหน้าจอของ PO ก็ตายตามไปด้วย

### ใช้ร่วมกัน — เขียนครั้งเดียว ห้ามมีสองชุด

| ชั้น | ของที่อยู่ในนั้น |
|---|---|
| ingest | Slack events · transcript · YouTrack webhook |
| **linker** | ladder 4 ชั้นใน §2 + ตาราง override ของมนุษย์ |
| ledger | `records` `embeddings` `work_items` `links` `relations` `decisions` |
| retrieval | `retrieve.py` (dense + BM25 + anchor + thread) · `rerank.py` |
| state engine | `relations.py` + `digest.py` |
| **cue list** | ชุดเดียว ทั้งไทยและอังกฤษ แก้ที่เดียว ได้ผลทั้งสองโปรดักต์ |

**cue list เป็นปุ่มปรับที่มีแรงงัดสูงสุดในระบบ** และเป็นเหตุผลหลักที่ต้องใช้ร่วมกัน —
เริ่มที่ไทยราว 30 คำ อังกฤษราว 20 คำ แล้วโตจากที่พลาดจริง อย่าเอา LLM มาทำงานนี้
เพราะช้ากว่า แพงกว่า และอ่านไม่ได้ว่ามันตัดสินจากอะไร ซึ่งขัดกับ §2 ทั้งข้อ

### ของใครของใคร

| | Catchup (ทีม dev) | ตาม (PO) |
|---|---|---|
| ผู้ใช้หลัก | dev ทั้งทีม | PO + lead |
| หน่วยที่พูดถึง | work item — ticket **หรือ** cluster | ticket เป็นหลัก |
| ช่องทางหลัก | Slack (เขียน) + web digest (อ่าน) | Slack board + DM |
| เขียนกลับ YouTrack | **ไม่ทำ** อ่านเท่านั้น | ทำ แต่ต้องมีคนกดยืนยันพร้อมเห็น diff |
| drift detector | ไม่มี | มี — เป็นหัวใจของมัน |
| decision log / recall | ใช้ `supersedes` แค่กันของเก่าไม่ให้โผล่เป็น active | มี decision chain เต็มรูป |
| น้ำเสียง | แมว เล่นได้ (เล่ม 1 §2) | ตรง ไม่มีลูกเล่น |
| ตัวชี้วัดว่าสำเร็จ | dev เปิดเองไหม | PO เลิกใช้ความจำตัวเองไหม |

### กฎกันเอกสารสองเล่มเดินหนีกัน

1. เรื่องในชั้น "ใช้ร่วมกัน" เขียนใน**เอกสารเดียว** อีกเล่มอ้างถึง — ห้ามก็อปข้าม
2. เพิ่มฟีเจอร์ที่แตะ ledger หรือ linker ต้องอัปเดตตารางข้างบนในรอบแก้เดียวกัน
3. ห้าม slash command ชนกัน — `/meow*` `/hiss` `/nap` `/yarn` เป็นของ Catchup ·
   `/tam*` เป็นของตาม
4. §9 guardrails ใช้กับทั้งสองตัว ไม่มีข้อยกเว้นให้ฝั่ง PO

### เรื่อง Geekbot — สองเอกสารไม่ตรงกัน และควรรู้ว่าต่างที่ไหน

§12 ของเล่มนี้บอกว่าเทมเพลตแบบ Geekbot คือ "ทางเข้าที่คนคุ้น" ที่ควรลอก
ส่วน `tam-proposal.md` §7 เสนอให้ **ซื้อ standup bot มาใช้ตอนนี้เลย** เพื่อดับความเจ็บ
เรื่องเรียกชื่อวนโต๊ะภายในไม่กี่วัน แล้วค่อยดูดงานนั้นเข้ามาทีหลัง

สองอันนี้ไม่ขัดกันจริง — เป็นคำถามเรื่อง**ลำดับ** ไม่ใช่เรื่องทิศทาง และคำตอบขึ้นกับข้อเดียว
ถ้าความเจ็บเรื่อง standup ต้องหายภายในสัปดาห์นี้ → ซื้อ แล้วเราไปสร้างของที่ของซื้อทำไม่ได้
ถ้ารอได้ 3–4 สัปดาห์ → ทำเองตั้งแต่ต้น จะได้ pre-fill กับหลักฐานที่ Geekbot ไม่มี
**ที่ห้ามทำคือปล่อยค้างไว้ไม่ตัดสิน** เพราะเป็นทางเดียวที่จะได้ทั้งค่าใช้จ่ายและความเจ็บพร้อมกัน

---

## 14. สรุปสำหรับคนที่อ่านแค่หน้านี้

- ผิวสามชั้น กฎเดียว: **Slack เขียน · Web อ่าน · Dashboard ลงมือ** ไม่ทำซ้ำกัน
- หน่วยกลางคือ **work item** ไม่ใช่ข้อความ และสถานะของมัน**ถูกคำนวณจาก typed
  relation ล่าสุด** พร้อมหลักฐานที่แตะได้ในหนึ่งคลิก — นี่คือสินค้า
- Slack tools แบ่งเป็น capture / ambient / delivery และ **ชั้น ambient คือชั้นที่
  ทำให้ต่างจาก standup bot** เพราะคนที่ไม่พิมพ์คำสั่งเลยก็ยังถูกนับ
- เว็บมีสี่หน้าอยู่แล้วในรีโป (`server.py`) พร้อม JSON API คู่ทุกหน้า — เปลี่ยน
  frontend ได้โดยไม่แตะ logic
- Dashboard เป็น **คิวที่ทุกแถวลงมือได้** ห้ามกลายเป็นกระดานคะแนนรายคน
- กุญแจของ work item เป็นแบบ**ผสม**: `ticket:REV-1421` เมื่อมี ticket key,
  `cluster:<id>` เมื่อไม่มี และ promote จาก cluster เป็น ticket ได้ — กองที่ผูกไม่ได้
  ต้องมองเห็นใน digest เสมอ (§2)
- **แยกเป็นสองโปรดักต์ backend เดียว** Catchup = ทีม dev · *ตาม* = PO
  ของที่ใช้ร่วมกันคือ ingest, linker, ledger, retrieval, state engine, cue list (§13)
- สิ่งที่ต้องรื้อ/สร้างก่อนอย่างอื่น: **linker** (ยังไม่มีเลย และเป็นชิ้นที่ตัดสินความน่าเชื่อ
  ทั้งระบบ), `State.rebuild()` ที่เป็น single-tenant, manifest ที่ยัง read-only,
  และ auth ที่ยังไม่มี
- ประตูของ Phase 0 เป็นตัวเลข: **ความแม่นของ linker ≥ 90% บน 200 ข้อความที่ label มือ**
  ผ่านด้วยวันที่ไม่ได้ (§10)
